[project]
name = "esmini-wrapper"
version = "0.1.0"
description = "An esmini wrapper for SBSVS"
readme = "README.md"
requires-python = "==3.10.*"
dependencies = [
    "pisa-api>=0.1.20"
]

[tool.uv.sources]
pisa-api = { git = "https://github.com/PISA-Hut/pisa-api.git" }
